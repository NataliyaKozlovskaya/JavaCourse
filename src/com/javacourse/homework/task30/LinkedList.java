//package com.javacourse.homework.task30;
//
//public class LinkedList {
//
//    String fist = null;
//    String last = null;
//    int sizeFist = 0;
//    int index = 0;
//    String prev=null;
//    String next=null;
//
//    public LinkedList(int defaultCapacity) {
//        this.defaultCapacity = defaultCapacity;
//    }
//
//    public void add(T elem){
//        if (array.length==sizeFist){
//            array[index]=elem;
////            fist = "ссылка на elem[0]";
////            last = "ссылка на elem[0]";
////            sizeFist++;
////            index++;
////        }else {
////            array[index-1] = elem;
////            prev = array[index-2];
////            next = array[index];
////            for (int i = index; i<= array.length; i++){
////                array[index]=array[index+1];
////            }
////            sizeFist++;
////        }
////    }
////
//////    public T get(int index) {
//////        T el;
//////        if (index < 0 && index >= array.length - 1) {
//////            String errorMessage = String.format("Данный индекс %s превышает длину списка: %s", array[index], array.length)
//////            throw new IndexOutOfBoundsException(errorMessage);
//////        } else {
//////            el = array[index];
////        }
////        return el;
////    }
////
////    public void remove(int index){
////        T el;
////        if (index < 0 && index >= array.length - 1) {
////            String errorMessage = String.format("Данный индекс %s превышает длину списка: %s", array[index], array.length)
////            throw new IndexOutOfBoundsException(errorMessage);
////        } else {
////            el = newEl;
////            prevnew=?
////            nextnew= ?
////            prevnew=null;
////            nextnew=null;
////            size--;
////            newEl = null;
////            el=?
////        }
////    }
//    public int size(){
//        return array.length;
//    }
//}
